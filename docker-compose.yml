services:
  web:
    build:
      context: .
      args:
        BUN_INSECURE_TLS: "${BUN_INSECURE_TLS:-0}"
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-8080}:80"
    networks:
      - aria-net

networks:
  aria-net:
    external: true
